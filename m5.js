const number =5;
for (let i = 0; i = number; i++) {
    if (number[i] % 5 === 0) {
        console.log(number," is divisible by 5");
    } else {
        console.log(number, " is not divisible by 5");
    }
}